import React from 'react';
import BigText from '../components/BigText';

const Board = () => {
    return (
        <div>
            <BigText>공지사항을 만들 예정</BigText>
        </div>
    );
};

export default Board;